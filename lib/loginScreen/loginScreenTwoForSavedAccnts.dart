import 'package:flutter/material.dart';

class LoginScreenTwoForSavedAccnts extends StatelessWidget {
  const LoginScreenTwoForSavedAccnts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [Text('value is present')],
        ),
      ),
    );
  }
}
